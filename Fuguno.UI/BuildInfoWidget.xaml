<UserControl x:Class="Fuguno.UI.BuildInfoWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="95.279" Width="Auto" Background="#FF5B5959">
    <Grid x:Name="Grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Label Name="BuildNumberLabel"  
               Grid.Column="1" 
               Grid.Row="0" 
               Content="{Binding BuildNumber}" 
               Height="Auto" 
               Width="Auto" 
               HorizontalAlignment="Left" 
               VerticalAlignment="Top" 
               Foreground="Black"/>
        <Label Name="RequestedForLabel" 
               Grid.Column="1" 
               Grid.Row="1" 
               Content="{Binding RequestedFor}" 
               Height="50" 
               Width="Auto" 
               HorizontalAlignment="Left"  
               VerticalAlignment="Top" 
               Foreground="Black"/>
    </Grid>
</UserControl>
